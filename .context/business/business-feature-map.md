> **Generated by**: `/business-feature-map` (QA Engineering repo — synthesized from API routes, UI pages, DB schema, components, PBI context)
> **Last update**: 2026-08-13
> **Previous version**: 2026-06-13
> **Sources**: `upex-bunkai-tms/app/api/v1/` (API routes), `upex-bunkai-tms/app/` (UI pages), `upex-bunkai-tms/components/` (UI components), `upex-bunkai-tms/lib/` (business logic), `upex-bunkai-tms/package.json` (integrations), `.context/business/business-data-map.md` (cross-referenced), `.context/PBI/` (stories, epics, bugs)
> **Scope**: Full-system feature inventory — QA view focused on what is IMPLEMENTED vs PLANNED
> **⚠️ Branch note**: verified against `staging` HEAD (`b9f3fc6`). `origin/main` is 697 commits behind — nothing below marked "new since June" exists on `main`. See `business-data-map.md` for the full branch-divergence discussion.
> **Regenerate with**: `/business-feature-map /home/lflores/proyectos/cursos/dojo3/upex-bunkai-tms`

# Business Feature Map: Bunkai TMS (QA View)

```
╔══════════════════════════════════════════════════════════════════════════════════╗
║ BUNKAI — BUSINESS FEATURE MAP (QA ENGINEERING VIEW)                              ║
║ 55 features identified across 12 domains (was 41 across 7 in June).              ║
║ 48 Stable | 2 Partial | 0 In Test | ~5 Planned/gap                               ║
║ Manual Execution, Bugs, Traceability, Notifications, Milestones — ALL shipped.   ║
╚══════════════════════════════════════════════════════════════════════════════════╝
```

---

## 1. Inventory Summary

| Category | Count | Status |
|---|---|---|
| **Auth & Identity** | 6 | Stable |
| **Workspace Management** | 6 | Stable |
| **Project Taxonomy** | 6 | Stable |
| **ATC Library** | 9 | Stable |
| **Tests (Chains)** | 5 | Stable |
| **Manual Execution** 🆕-live | 6 | Stable (was 100% Planned/Backlog in June) |
| **Defects (Bugs)** 🆕 | 4 | Stable |
| **Traceability** 🆕 | 3 | Stable |
| **Notifications** 🆕 | 3 | Stable |
| **Milestones** 🆕 | 2 | Stable |
| **Account & Settings** | 5 | Stable (1 partial — "Coming Soon" placeholders) |
| **Platform / Ops / Dashboard** | 5 | Stable, 1 corrected to non-functional |

| Status | Count |
|---|---|
| ✅ Stable (fully implemented) | 48 |
| ⚠️ Partial (implemented, gap or dead) | 2 (Settings "Coming Soon" nav, Feature Flags dead table) |
| ❌ Not implemented despite doc claims | 1 (Feature Flags — see FEAT-042 correction) |
| 📋 Discovery Gap (planned, unclear scope) | 4 (see §9) |

> **Security Alert — RESOLVED**: BK-134/BK-135 (member-role self-issuance of `workspace:admin` PATs) — **fixed**. See FEAT-005.
> **Security Alert — RESOLVED**: BK-316 (Bearer/PAT ignored active-workspace switch) — **fixed** by rejection (403), not by making Bearer respect the switch. See FEAT-009.

---

## 2. Feature Catalog

### Domain: Auth & Identity

---

#### Feature: Email Magic Link Authentication

| Aspect | Value |
|---|---|
| **ID** | FEAT-001 |
| **Status** | ✅ Stable |
| **Endpoints** | `POST /api/v1/auth/magic-link` |
| **UI** | `app/(auth)/login/magic-link-form.tsx`, `magic-link-disclosure.tsx` |
| **Users** | Public (pre-auth) |
| **Evidence** | `app/api/v1/auth/magic-link/route.ts` |

**Capabilities:**
- [x] Send magic link email, custom (not Supabase-native) token, single-use
- [x] Consume via `app/auth/callback/route.ts`
- [ ] Cross-device use — attempted (BK-400) then **explicitly reverted** as "the dangerous part"; still same-device/PKCE only

---

#### Feature: Password Sign-in (Headless)

| Aspect | Value |
|---|---|
| **ID** | FEAT-002 |
| **Status** | ✅ Stable |
| **Endpoints** | `POST /api/v1/auth/signin` |
| **UI** | `app/(auth)/login/email-first-form.tsx` (password step) |
| **Users** | Public (pre-auth) |
| **Evidence** | `app/api/v1/auth/signin/route.ts` |

**Capabilities:**
- [x] Email + password sign-in
- [x] Auto-mints a PAT (`DEFAULT_PAT_SCOPES`, excludes `workspace:admin`) in the same response — headless-friendly

---

#### Feature: Email Sign-up + Mandatory OTP Confirmation (BK-166)

| Aspect | Value |
|---|---|
| **ID** | FEAT-003 |
| **Status** | ✅ Stable — **UI substantially changed since June** |
| **Endpoints** | `POST /api/v1/auth/check-email`, `POST /api/v1/auth/signup`, `POST /api/v1/auth/confirm`, `POST /api/v1/auth/resend` |
| **UI** | `app/(auth)/login/email-first-form.tsx` — single component, 3-step state machine (email → password → OTP verify) |
| **Users** | Public (pre-auth) |
| **Evidence** | `app/api/v1/auth/confirm/route.ts`, `email-first-form.tsx:394-448` |

**Capabilities:**
- [x] Email-first routing (check-email decides sign-in vs sign-up path)
- [x] 🆕 Mandatory 6-digit OTP confirmation step before account is usable — no bypass
- [x] Resend code
- [x] On confirm: mints session + PAT
- **Discovery**: no standalone `/signup` route found — folded entirely into this one component's state machine, not a separate page (was implied separate in June's doc, worth correcting future assumptions)

---

#### Feature: OAuth Sign-in (Google / GitHub)

| Aspect | Value |
|---|---|
| **ID** | FEAT-004 |
| **Status** | ✅ Stable |
| **Endpoints** | `app/auth/oauth/[provider]/route.ts` |
| **UI** | `app/(auth)/login/oauth-buttons.tsx` |
| **Evidence** | Unchanged since June |

---

#### Feature: Personal Access Token (PAT) Management

| Aspect | Value |
|---|---|
| **ID** | FEAT-005 |
| **Status** | ✅ Stable — **security fix landed** |
| **Endpoints** | `GET/POST /api/v1/tokens`, `DELETE /api/v1/tokens/{id}` |
| **UI** | `components/settings/{IssueTokenModal,RevokeTokenModal,TokensList}.tsx` |
| **Evidence** | `lib/api/pat.ts` |

**Capabilities:**
- [x] Issue (session-only — PATs cannot mint PATs), list, revoke (soft-delete)
- [x] **BK-134/135 FIXED**: `assertTokenIssuanceAuthorized` requires admin/owner role for `workspace:admin` scope before minting — member-role self-issuance no longer possible
- [x] **New**: `assertNoGlobalAdminScope` hard-rejects a global (`workspace_id=NULL`) admin-scoped PAT on any headless issuance path
- [x] **New**: `assertWorkspaceContext` binds an admin-scoped PAT to its issuing workspace at call time, not just at mint time

---

#### Feature: User Profile Introspection

| Aspect | Value |
|---|---|
| **ID** | FEAT-006 |
| **Status** | ✅ Stable |
| **Endpoints** | `GET /api/v1/me`, `POST /api/v1/me/active-workspace` |
| **Evidence** | Unchanged shape since June; see FEAT-009 for the switch-endpoint security fix |

---

### Domain: Workspace Management

---

#### Feature: Workspace Creation

| Aspect | Value |
|---|---|
| **ID** | FEAT-007 |
| **Status** | ✅ Stable |
| **Endpoints** | `POST /api/v1/workspaces` → `bunkai_bootstrap_workspace` RPC |
| **UI** | `app/(app)/onboarding/page.tsx` 🆕 (first-run form; June's doc did not document an onboarding UI) |

---

#### Feature: Workspace Read & Update

| Aspect | Value |
|---|---|
| **ID** | FEAT-008 |
| **Status** | ✅ Stable |
| **Endpoints** | `GET/PATCH /api/v1/workspaces/{id}` (PATCH requires `workspace:admin`) |

---

#### Feature: Workspace Switching

| Aspect | Value |
|---|---|
| **ID** | FEAT-009 |
| **Status** | ✅ Stable — **security fix landed (BK-316)** |
| **Endpoints** | `POST /api/v1/me/active-workspace` |
| **UI** | `components/layout/WorkspaceSwitcher.tsx` |
| **Evidence** | `app/api/v1/me/active-workspace/response.ts:42-49` |

**Capabilities:**
- [x] Session-authenticated switch, sets `bk_active_ws` cookie
- [x] **BK-316 FIXED**: Bearer/PAT callers now get a hard `403 forbidden` (`assertSessionOnly`) instead of the switch silently no-op'ing. A PAT's workspace binding is fixed at mint time and was never meant to be switchable — this was a rejection fix, not a "make it work" fix.

---

#### Feature: Workspace Invite Management

| Aspect | Value |
|---|---|
| **ID** | FEAT-010 |
| **Status** | ✅ Stable |
| **Endpoints** | `GET/POST /api/v1/workspaces/{id}/invites`, `POST/DELETE .../invites/{inviteId}` (all `workspace:admin`) |
| **Evidence** | Unchanged — still no email send, see GAP-5 in §9 |

---

#### Feature: Invite Acceptance

| Aspect | Value |
|---|---|
| **ID** | FEAT-011 |
| **Status** | ✅ Stable |
| **Endpoints** | `POST /api/v1/invites/accept` |
| **UI** | `app/invites/accept/page.tsx` |

---

#### Feature: 🆕 Workspace Member Management

| Aspect | Value |
|---|---|
| **ID** | FEAT-043 |
| **Status** | ✅ Stable |
| **Endpoints** | `DELETE /api/v1/workspaces/{id}/membership` (leave) |
| **UI** | `app/(app)/workspaces/[id]/members/page.tsx` |
| **Evidence** | New page not present in June's inventory |

**Capabilities:**
- [x] View workspace members and roles
- [x] Leave workspace (self)

---

### Domain: Project Taxonomy

---

#### Feature: Project View

| Aspect | Value |
|---|---|
| **ID** | FEAT-012 |
| **Status** | ✅ Stable |
| **Endpoints** | `POST /api/v1/workspaces/{id}/projects` |
| **UI** | `app/(app)/projects/page.tsx`, `projects/new/page.tsx`, `projects/[projectSlug]/page.tsx` |

---

#### Feature: Module Hierarchy

| Aspect | Value |
|---|---|
| **ID** | FEAT-013 |
| **Status** | ✅ Stable |
| **Endpoints** | `POST /api/v1/projects/{id}/modules`, `PATCH/DELETE /api/v1/modules/{id}` |
| **UI** | `create-module-form.tsx`, `rename-module-form.tsx`, `move-module-dialog.tsx`, `delete-module-dialog.tsx` |
| **Evidence** | Now confirmed RPC-backed: `bunkai_update_module` / `bunkai_move_module` / `bunkai_archive_module_subtree` — same shape, more certainty than June |

---

#### Feature: User Story Management

| Aspect | Value |
|---|---|
| **ID** | FEAT-014 |
| **Status** | ✅ Stable |
| **Endpoints** | `POST/GET /api/v1/modules/{id}/user-stories`, `GET/PATCH/DELETE /api/v1/user-stories/{id}` |
| **Evidence** | `ready_to_test` transition now confirmed via explicit `bunkai_ready_to_test_gate` RPC |

---

#### Feature: Acceptance Criteria Management

| Aspect | Value |
|---|---|
| **ID** | FEAT-015 |
| **Status** | ✅ Stable |
| **Endpoints** | `GET/POST /api/v1/user-stories/{id}/acceptance-criteria`, `GET/PATCH/DELETE /api/v1/acceptance-criteria/{id}` |

---

#### Feature: 🆕 Project Environments

| Aspect | Value |
|---|---|
| **ID** | FEAT-044 |
| **Status** | ✅ Stable |
| **Endpoints** | `GET/POST /api/v1/projects/{id}/environments`, `PATCH/DELETE /api/v1/environments/{id}` |
| **UI** | `create-environment-form.tsx` |
| **Evidence** | `project_environments` table, `supabase/migrations/0031_runs.sql` |

**Capabilities:**
- [x] Named deploy targets (staging/prod) a Run executes against — prerequisite for Manual Execution domain below

---

### Domain: ATC Library

*(unchanged shape since June — 9 features, all still Stable. Consolidated here since no structural changes found this round.)*

| ID | Feature | Endpoints | UI |
|---|---|---|---|
| FEAT-016 | ATC REST API (Create & Edit) | `POST /api/v1/atcs` (RPC `bunkai_create_atc`), `PATCH /api/v1/atcs/{id}` (RPC `bunkai_update_atc`, `X-If-Match` lock) | — |
| FEAT-017 | ATC Builder UI | — | `components/atcs/{AtcEditor,NewAtcEditor,StepEditor,AtcPreview}.tsx` |
| FEAT-018 | ATC Anchoring | — | `AnchoringPanel.tsx` |
| FEAT-019 | Project Explorer (Sidebar Tree) | — | `project-explorer.tsx`, `mind-map-view.tsx` |
| FEAT-020 | ATC Table View | — | `AtcTable.tsx` |
| FEAT-021 | ATC Search & Autocomplete | `GET /api/v1/atcs/search` | — |
| FEAT-022 | ATC Edit Propagation | (returns `affected_test_count`, BK-21) | — |
| FEAT-023 | ATC Usage Report | `GET /api/v1/atcs/{id}/usage` | — |
| FEAT-024 | ATC Duplicate | `POST /api/v1/atcs/{id}/duplicate` | — |

**Correction (applies to FEAT-016)**: June's doc claimed ATC creation had moved to an "app-layer transaction" after the `bunkai_save_atc` RPC was "removed." **Both claims were false** — the RPC was never removed and creation has always been RPC-backed. See `business-data-map.md` §2 for the full correction.

---

### Domain: Tests (Chains)

| ID | Feature | Endpoints | UI |
|---|---|---|---|
| FEAT-025 | Test Builder (Assemble ATC Chain) | `POST /api/v1/tests` | `NewTestBuilder.tsx`, `AtcChainPicker.tsx` |
| FEAT-026 | ATC Reorder Inside Test | `PATCH /api/v1/tests/{id}/reorder` | `TestReorderClient.tsx` |
| FEAT-027 | Test View (Expanded ATC Chain) | `GET /api/v1/tests/{id}` | `TestDetailView.tsx`, `TestDetailTabs.tsx` |
| FEAT-028 | Test Tags | `PUT /api/v1/tests/{id}/tags` | `TestTagEditor.tsx` |
| FEAT-045 🆕 | Per-Test Run History | `GET /api/v1/tests/{id}/runs` | `app/.../tests/[testId]/runs/page.tsx` |

---

### Domain: Manual Execution 🆕 — shipped (was 100% Planned/Backlog in June)

Every feature in this domain was documented in June as `⏳ Backlog` or `🔄 Ready For Dev` with all capability checkboxes unchecked. All six are now live (BK-182 epic).

---

#### Feature: Manual Run Start

| Aspect | Value |
|---|---|
| **ID** | FEAT-029 |
| **Status** | ✅ Stable (was 🔄 Ready For Dev) |
| **Endpoints** | `POST /api/v1/runs` (`run:execute` scope) |
| **UI** | `components/tests/StartRunButton.tsx`, environment picker |
| **Evidence** | `supabase/migrations/0031_runs.sql`, `runs/route.ts:46-140` |

**Capabilities:**
- [x] Start a manual Run for a Test against a chosen Environment
- [x] Snapshot every executable step as "pending" at creation (`run_atcs`/`run_steps`, denormalized titles survive later ATC edits)
- [x] Idempotent — `Idempotency-Key` header REQUIRED, `start_token` 24h dedup window prevents double-start
- [x] `resolveRunWorkspaceId` resolves the correct workspace for Bearer callers with no active-workspace cookie (BK-182 — deliberately not touched by the later BK-316 fix)

---

#### Feature: Step Result Update

| Aspect | Value |
|---|---|
| **ID** | FEAT-030 |
| **Status** | ✅ Stable (was ⏳ Estimation) |
| **Endpoints** | `POST /api/v1/runs/{id}/steps/{stepId}/mark` |
| **UI** | Per-step Pass/Fail/Blocked controls + note/evidence-url in `RunnerView.tsx` |

**Capabilities:**
- [x] Mark each run step pass/failed/blocked/skipped, with optional evidence

---

#### Feature: Run Abort

| Aspect | Value |
|---|---|
| **ID** | FEAT-031 |
| **Status** | ✅ Stable (was ⏳ Estimation) |
| **Endpoints** | `POST /api/v1/runs/{id}/abort` |
| **UI** | Abort modal (reason required) in `RunnerView.tsx` |

**Capabilities:**
- [x] Abort an in-progress Run with a reason — `abort_reason` enforced by a **DB CHECK constraint** (`runs_abort_reason_chk`), not just a frontend validation

---

#### Feature: Run History

| Aspect | Value |
|---|---|
| **ID** | FEAT-032 |
| **Status** | ✅ Stable (was ⏳ Backlog) |
| **Endpoints** | `GET /api/v1/tests/{id}/runs` |
| **UI** | `app/.../runs/page.tsx` |

---

#### Feature: Run Reporting

| Aspect | Value |
|---|---|
| **ID** | FEAT-033 |
| **Status** | ✅ Stable (was ⏳ Backlog) |
| **Endpoints** | `GET /api/v1/projects/{id}/runs/report` |

---

#### Feature: Run Final Verdict

| Aspect | Value |
|---|---|
| **ID** | FEAT-034 |
| **Status** | ✅ Stable (was ⏳ Backlog) |
| **Endpoints** | `POST /api/v1/runs/{id}/finish` |
| **UI** | Finish/verdict modal in `RunnerView.tsx` |

**Capabilities:**
- [x] Finish a Run with a final verdict (passed/failed/aborted)
- [x] Remaining `pending` steps auto-marked `skipped` on finish
- [x] Fires a DB trigger (`activity_log_notify_run_event`) → writes a `notifications` row on `run.finished`/`run.aborted`

---

### Domain: Defects (Bugs) 🆕

*New domain — not present in June's doc at all (the `bugs` table was `❌ Not yet in DB`).*

---

#### Feature: Bug Reporting

| Aspect | Value |
|---|---|
| **ID** | FEAT-046 |
| **Status** | ✅ Stable |
| **Endpoints** | `POST /api/v1/bugs` (`atc:write` scope) |
| **UI** | `components/bugs/BugFormDialog.tsx` |
| **Users** | `role !== viewer` |
| **Evidence** | `supabase/migrations/0046_bugs.sql` |

**Capabilities:**
- [x] File a bug from a failed run step (title, severity, module, description, steps-to-reproduce, evidence URLs)
- [x] DB trigger `bugs_check_consistency` validates the run_step/atc/run/project all belong together before insert is allowed — data-integrity guard, not just app-layer validation

---

#### Feature: Bug List & Filtering

| Aspect | Value |
|---|---|
| **ID** | FEAT-047 |
| **Status** | ✅ Stable |
| **Endpoints** | `GET /api/v1/bugs`, `GET /api/v1/projects/{id}/bugs` |
| **UI** | `components/bugs/BugsListView.tsx` |

**Capabilities:**
- [x] Filter by module/status/severity

---

#### Feature: Bug Assignment & Status Transition

| Aspect | Value |
|---|---|
| **ID** | FEAT-048 |
| **Status** | ✅ Stable |
| **Endpoints** | `POST /api/v1/bugs/{id}/assign`, `POST /api/v1/bugs/{id}/status` (both `atc:write`) |
| **UI** | Assign/reassign/unassign controls in `BugsListView.tsx` |

**Capabilities:**
- [x] Assign/reassign/unassign
- [x] Status transitions are **forward-only** (BK-264)
- [x] Assign/reassign/unassign fires `activity_log_notify_bug_event` trigger → `notifications` row

---

#### Feature: Bug Heatmap & Recovery Metrics

| Aspect | Value |
|---|---|
| **ID** | FEAT-049 |
| **Status** | ✅ Stable |
| **Endpoints** | `GET /api/v1/projects/{id}/bugs/heatmap`, `GET /api/v1/projects/{id}/metrics/recovery-cycles` |
| **UI** | `app/.../metrics/page.tsx` |

**Capabilities:**
- [x] Module-level bug density heatmap
- [x] Recovery-cycle metrics (time from bug filed to resolved, by module)

---

### Domain: Traceability 🆕

*New domain — BK-45/48/50 epic, not present in June's doc.*

---

#### Feature: Traceability Chain Report

| Aspect | Value |
|---|---|
| **ID** | FEAT-050 |
| **Status** | ✅ Stable |
| **Endpoints** | `GET /api/v1/projects/{id}/traceability?story={id}` (RPC `bunkai_report_story_traceability`) |
| **UI** | `components/traceability/TraceabilityChainView.tsx`, `app/.../traceability/page.tsx` |
| **Evidence** | `supabase/migrations/0068_story_traceability_report.sql` |

**Capabilities:**
- [x] Assembles the full chain: User Story → ATC → Test → Run → Bug, in one read
- [x] Non-disclosing on error: missing/foreign/wrong-project story ID all collapse to the same 404 (never 403 — deliberate anti-enumeration design)
- [x] Read-only — no mutation, no DB table of its own (pure report, assembled live from existing tables)

---

#### Feature: Traceability Filters (BK-48)

| Aspect | Value |
|---|---|
| **ID** | FEAT-051 |
| **Status** | ✅ Stable |
| **UI** | `FilterBar` inside `TraceabilityChainView.tsx:124-261` |

**Capabilities:**
- [x] Filter by result (multi-toggle), module, date range
- [x] Active-filter chips + Clear

---

#### Feature: Traceability Export (BK-50)

| Aspect | Value |
|---|---|
| **ID** | FEAT-052 |
| **Status** | ✅ Stable |
| **UI** | Export button, `TraceabilityChainView.tsx:92,430,612-618` |
| **Evidence** | `lib/traceability/export-snapshot.ts:19-33` |

**Capabilities:**
- [x] Downloads the assembled chain as a self-contained `.html` snapshot
- [x] **100% client-side** — no server storage, no upload, no new table (deliberate design per Jira comments 12238/12239: "Option E, synchronous client-initiated download")

---

### Domain: Notifications 🆕

*New domain — BK-209.*

---

#### Feature: Notification Feed

| Aspect | Value |
|---|---|
| **ID** | FEAT-053 |
| **Status** | ✅ Stable |
| **Endpoints** | `GET/POST /api/v1/workspaces/{id}/notifications`, `.../read-all`, `POST /api/v1/notifications/{id}/read` |
| **UI** | Bell trigger + `components/notifications/{NotificationsPanel,NotificationRow}.tsx` in `AppSidebar.tsx` |
| **Evidence** | `lib/notifications/realtime-notifications-channel.ts` — reuses the Run channel's reconnect/scheduler logic (DRY note in source) |

**Capabilities:**
- [x] Realtime bell + toast (Supabase Realtime channel on `notifications` table)
- [x] Mark one / mark-all read
- [x] Click-through deep link via `resolveNotificationHref`
- [x] Written entirely by DB triggers (`activity_log_notify_bug_event`, `activity_log_notify_run_event`) — not app code

---

#### Feature: Notification Preferences

| Aspect | Value |
|---|---|
| **ID** | FEAT-054 |
| **Status** | ✅ Stable |
| **Endpoints** | `GET/PATCH /api/v1/notification-preferences` |
| **UI** | `components/settings/NotificationPreferencesGrid.tsx` |

---

### Domain: Milestones 🆕

---

#### Feature: Milestone Management

| Aspect | Value |
|---|---|
| **ID** | FEAT-055 |
| **Status** | ✅ Stable |
| **Endpoints** | `GET/POST /api/v1/projects/{id}/milestones`, `PATCH/DELETE /api/v1/milestones/{id}` |
| **UI** | `components/milestones/{MilestonesListView,CreateMilestoneForm,EditMilestoneForm,MilestoneDetailView}.tsx` |
| **Users** | `role !== viewer` to create; viewers see a read-only note instead of the New button |

**Capabilities:**
- [x] Create/edit a project-level target date + description
- [x] List with countdown badge; detail view

---

### Domain: Account & Settings

---

#### Feature: Account Identity & Sign Out — FEAT-035 · Settings Hub — FEAT-036 · PAT Management UI — FEAT-037 (see FEAT-005) · Workspace List (Settings) — FEAT-038 · Leave Workspace — FEAT-039 (see FEAT-043)

All ✅ Stable, unchanged in shape since June. Evidence: `components/settings/{IdentityCard,SettingsNav,WorkspacesList,LeaveWorkspaceModal}.tsx`.

**🆕 Discovery — "Coming Soon" placeholder nav**: `lib/settings/nav-items.ts:14-27` hardcodes `SETTINGS_NAV_COMING_SOON` = Members, Billing, Environments — inert nav entries (`href: null`, non-focusable). This is the **only genuine planned-but-unbuilt UI signal found in the entire codebase** this round (no TODO/FIXME comments, no feature-flag gating exist anywhere else). Note: a Members page and an Environments feature (FEAT-043, FEAT-044) DO now exist elsewhere in the app (workspace-level members page, project-level environments) — this stale "coming soon" nav entry may simply not have been cleaned up after those shipped. Worth a dev-team question, not a QA blocker.

---

### Domain: Platform / Ops / Dashboard

---

#### Feature: API Documentation — FEAT-040 · Software Testability Guide — FEAT-041

Unchanged, ✅ Stable.

---

#### Feature: Feature Flags — FEAT-042 — **CORRECTED: not functional**

June's doc listed this as "DB-only" (table exists, implying eventual use). This update found it is **fully dead**: `feature_flags` table exists in the generated schema (`lib/types/supabase.ts:441`) but is queried **zero times** anywhere in the codebase (`grep .from('feature_flags')` = 0 hits). There is no feature-flag system in this product — none of the standard signals exist either (no `FEATURE_`/`ENABLE_`/`BETA_` env vars, no flag-reading code). Downgrade from "⚠️ Partial / DB-only" to "❌ Not functional — orphaned table."

---

#### Feature: 🆕 Home Dashboard

| Aspect | Value |
|---|---|
| **ID** | FEAT-056 |
| **Status** | ✅ Stable |
| **UI** | `app/(app)/home/page.tsx` + `components/home/{ActiveRuns,CoverageSummary,OpenBugs,RecentActivity,RecentProjects,WelcomeBanner}.tsx` |
| **Endpoints** | `GET /api/v1/workspaces/{id}/{coverage,open-bugs,active-runs,recent-projects}` |

**Capabilities:**
- [x] Landing dashboard aggregating active runs, coverage %, open bugs, recent activity, recent projects — did not exist as a described feature in June

---

#### Feature: 🆕 Project Coverage Dashboard

| Aspect | Value |
|---|---|
| **ID** | FEAT-057 |
| **Status** | ✅ Stable |
| **Endpoints** | `GET /api/v1/projects/{id}/coverage` |
| **UI** | `components/coverage/ProjectCoverageView.tsx` |

---

#### Feature: 🆕 Activity Feed

| Aspect | Value |
|---|---|
| **ID** | FEAT-058 |
| **Status** | ✅ Stable |
| **Endpoints** | `GET /api/v1/activity` |
| **UI** | `components/activity/ActivityView.tsx` |

---

## 3. CRUD Matrix

| Entity | Create | Read | Update | Delete | Evidence |
|---|---|---|---|---|---|
| Workspace | ✅ | ✅ | ✅ (admin) | ❌ (leave-only, no delete API) | `workspaces/route.ts` |
| Workspace Member | ✅ (via invite) | ✅ | ⚠️ (role change not confirmed) | ✅ (leave) | `workspaces/[id]/membership` |
| Project | ✅ | ✅ | ⚠️ (not confirmed this round) | ❌ | `workspaces/[id]/projects` |
| Project Environment | ✅ | ✅ | ✅ | ✅ | `projects/[id]/environments` |
| Module | ✅ | ✅ | ✅ (rename/move) | ⚠️ Soft (archive, one-way) | `modules/[id]` |
| User Story | ✅ | ✅ | ✅ | ⚠️ Soft (archive) | `user-stories/[id]` |
| Acceptance Criterion | ✅ | ✅ | ✅ (edit/reorder) | ⚠️ Soft (archive) | `acceptance-criteria/[id]` |
| ATC | ✅ (RPC) | ✅ | ✅ (RPC, full-replace) | ⚠️ Soft (archive) | `atcs/[id]` |
| Test | ✅ | ✅ | ✅ (reorder/tags) | ⚠️ (delete not confirmed this round) | `tests/[id]` |
| Run | ✅ | ✅ | ✅ (mark/abort/finish) | ❌ (no delete — audit trail) | `runs/[id]` |
| Bug | ✅ | ✅ | ✅ (assign/status) | ❌ (no delete found) | `bugs/[id]` |
| Milestone | ✅ | ✅ | ✅ | ✅ | `milestones/[id]` |
| PAT | ✅ | ✅ | ❌ | ⚠️ Soft (revoke) | `tokens/[id]` |
| Invite | ✅ | ✅ | ✅ (rotate) | ✅ (revoke) | `invites/[inviteId]` |
| Notification | ❌ (system-written only) | ✅ | ✅ (mark read) | ❌ | `notifications/[id]` |

Legend: ✅ Full · ⚠️ Partial/soft/conditional · ❌ Not available

---

## 4. API Endpoint Inventory

Full per-endpoint table (method, path, purpose, auth tier) — see `.context/business/business-api-map.md` for the narrative/journey framing of these same routes, and `business-data-map.md` §6 for the route-group summary. Auth model: every route runs through `withApiHandler` (`lib/api/handler.ts`) — default `auth: 'required'` (unified cookie-OR-Bearer resolution), `auth: 'public'` is an explicit opt-out (8 routes total), `requires: ['scope']` adds a capability gate on top.

| Domain | Endpoints | Auth |
|---|---|---|
| Discovery/Health | `GET /api/v1`, `GET /api/v1/health` | Public |
| Auth | `POST /auth/{signup,signin,confirm,check-email,magic-link,resend}` | Public |
| Identity | `GET /me`, `POST /me/active-workspace` | Required |
| Tokens | `GET/POST /tokens`, `DELETE /tokens/{id}` | Required (issuance session-only) |
| Workspaces | `GET/POST /workspaces`, `GET/PATCH /workspaces/{id}`, `DELETE .../membership`, `GET .../{coverage,open-bugs,active-runs,recent-projects}` | Required (PATCH: `workspace:admin`) |
| Invites | `GET/POST .../invites`, `POST/DELETE .../invites/{id}`, `POST /invites/accept` | Required: `workspace:admin` (issue/rotate/revoke) |
| Projects | `POST /workspaces/{id}/projects`, `GET/POST .../modules`, `.../milestones`, `.../environments` | Required |
| Reporting | `GET /projects/{id}/{bugs,bugs/heatmap,coverage,metrics/recovery-cycles,runs/report,traceability}` | Required (RLS-gated) |
| Modules | `PATCH/DELETE /modules/{id}`, `.../user-stories` | Required |
| User Stories / AC | `GET/PATCH/DELETE /user-stories/{id}`, `GET/POST .../acceptance-criteria`, `GET/PATCH/DELETE /acceptance-criteria/{id}` | Required |
| ATCs | `GET/POST /atcs` (write: `atc:write`), `PATCH /atcs/{id}`, `GET /atcs/search`, `/atcs/{id}/usage`, `POST /atcs/{id}/duplicate` | Required: `atc:read`/`atc:write` |
| Tests | `GET/POST /tests`, `GET /tests/{id}`, `.../runs`, `PATCH .../reorder`, `PUT .../tags` | Required: `atc:read`/`atc:write` |
| Runs | `POST /runs`, `GET /runs/{id}`, `POST .../abort`, `.../finish`, `.../steps/{id}/mark` | Required: `run:execute` |
| Bugs | `POST/GET /bugs`, `POST /bugs/{id}/assign`, `.../status` | Required: `atc:write` (write ops) |
| Notifications | `GET/POST /workspaces/{id}/notifications`, `.../read-all`, `POST /notifications/{id}/read`, `GET/PATCH /notification-preferences` | Required |
| Activity | `GET /activity` | Required |
| Imports | `POST/GET /imports`, `/imports/{id}` | Required |

---

## 5. UI Component Inventory

### Forms

| Component | Purpose |
|---|---|
| `email-first-form.tsx` | Login/signup/OTP 3-step state machine |
| `create-environment-form.tsx`, `create-module-form.tsx`, `import-from-jira-dialog.tsx` | Project sub-resource creation |
| `NewAtcEditor.tsx`, `StepEditor.tsx` | ATC authoring |
| `NewTestBuilder.tsx`, `AtcChainPicker.tsx` | Test assembly |
| `BugFormDialog.tsx` 🆕 | Bug report |
| `CreateMilestoneForm.tsx`, `EditMilestoneForm.tsx` 🆕 | Milestone CRUD |
| `IssueTokenModal.tsx` | PAT issuance |

### Dashboards / Views

| Component | Purpose |
|---|---|
| `app/(app)/home/page.tsx` 🆕 + `components/home/*` | Landing dashboard |
| `ProjectCoverageView.tsx` 🆕 | Coverage % visualization |
| `RunnerView.tsx` 🆕 | Run execution surface |
| `TraceabilityChainView.tsx` 🆕 | Traceability report + filters + export |
| `BugsListView.tsx` 🆕 | Bug tracker |
| `MilestonesListView.tsx`, `MilestoneDetailView.tsx` 🆕 | Milestones |
| `ActivityView.tsx` 🆕 | Activity feed |
| `AtcTable.tsx`, `project-explorer.tsx`, `mind-map-view.tsx` | ATC/module navigation |

### Actions (modals, dialogs, confirmations)

| Component | Purpose |
|---|---|
| `RevokeTokenModal.tsx`, `LeaveWorkspaceModal.tsx`, `delete-module-dialog.tsx`, `move-module-dialog.tsx` | Destructive/state-changing confirmations |
| Abort/Finish modals in `RunnerView.tsx` 🆕 | Run termination |
| `NotificationsPanel.tsx`, `NotificationRow.tsx` 🆕 | Notification bell panel |

---

## 6. Third-Party Integrations

| Service | Purpose | Package | Status | Features using it |
|---|---|---|---|---|
| Supabase Auth | OAuth, session, magic-link backing | `@supabase/*` | ✅ Live | FEAT-001–004 |
| Supabase Postgres + RLS | Primary datastore, multi-tenancy | `@supabase/*` | ✅ Live | All |
| Supabase Realtime | Live ATC/import/run/notification updates | `@supabase/*` | ✅ Live | FEAT-053 + ATC tree + import progress |
| Jira (Atlassian) | Story import | raw `fetch`, no SDK (`lib/jira/client.ts`) | ✅ Live | Import feature |
| Resend | Email delivery | *(none installed)* | ❌ **Never wired** — `RESEND_API_KEY` used only by the CLI installer, never by app code. See GAP-5. | — |
| Cloudflare R2 / MinIO | Evidence storage | *(none)* | ❌ Zero code wiring — architecture-doc only | — |
| Sentry | Error tracking | *(none)* | ❌ **Not implemented** — corrected from June's doc which implied it was live | — |
| PostHog | Analytics | *(none)* | ❌ **Not implemented** — same correction | — |

---

## 7. Feature Flags & WIP

**There is no functioning feature-flag system in this product.** Corrected finding (see FEAT-042): the `feature_flags` DB table exists but is never queried by any code path. No `FEATURE_`/`ENABLE_`/`BETA_` env vars exist in `.env.example` or code.

| Flag / mechanism | Description | Status |
|---|---|---|
| `feature_flags` table | Workspace-scoped toggle table | ❌ Orphaned — 0 read/write call sites found |
| `SETTINGS_NAV_COMING_SOON` (`lib/settings/nav-items.ts:14-27`) | Hardcoded inert nav entries: Members, Billing, Environments | Partial — Members and Environments features now actually exist elsewhere in the app; this list appears stale |

**TODO/FIXME/WIP scan**: zero hits across `app/api/**` and `lib/**` (excluding tests). No loose WIP markers anywhere in the codebase — a change from what might be assumed for a fast-moving repo. The one soft-TODO found is a documented design decision, not a marker: `app/api/v1/bugs/route.ts:8-23` keeps a superseded endpoint "functionally untouched" pending a future retirement tech-story.

**No stub/incomplete handlers found** — every route under `app/api/v1/` has a real implementation.

---

## 8. QA Relevance

### Feature test coverage matrix

Not re-derived from a test-runner report this round (out of scope for a business-feature-map regen — that's `/test-documentation` territory). Flagging as a Discovery Gap below rather than fabricating coverage numbers.

### High-risk features (prioritize testing)

| Feature | Risk | Reason |
|---|---|---|
| PAT issuance (FEAT-005) | HIGH | Recently patched security bug (BK-134/135) — regression-test the role/scope gate on every release |
| Workspace switching (FEAT-009) | HIGH | Recently patched security bug (BK-316) — regression-test the 403-for-Bearer behavior |
| Bug consistency trigger (FEAT-046) | MEDIUM | DB-level integrity guard (`bugs_check_consistency`) — verify it can't be bypassed via a crafted payload |
| Run abort reason constraint (FEAT-031) | MEDIUM | Enforced at DB layer (`runs_abort_reason_chk`) — verify both API and DB layers agree, not just the frontend form |
| Traceability non-disclosure (FEAT-050) | MEDIUM | Deliberately returns 404 (never 403) for cross-workspace story access — verify this holds under all invalid-ID shapes, not just the happy-path 404 |
| Email delivery (invite flow) | MEDIUM (business risk, not security) | Still MVP-scoped as out-of-band URL sharing — confirm this is still the accepted product decision, not a forgotten gap |

---

## 9. Discovery Gaps

- **GAP-5 (carried from June, still open)**: Email delivery for invites never wired. `RESEND_API_KEY` unused by app code.
- **🆕 GAP: CRUD matrix has 4 unconfirmed cells** (Workspace Member role-change, Project update/delete, Test delete) — not explicitly re-verified this round; flagged rather than guessed in §3.
- **🆕 GAP: "Coming Soon" settings nav appears stale**, not a genuine roadmap signal — Members and Environments features already shipped elsewhere. Worth a one-line question to the dev team rather than treating it as a real gap.
- **🆕 GAP: No automated feature-test-coverage matrix generated this round** — §8's coverage matrix was intentionally left unfilled rather than fabricated; run `/test-documentation` for the real numbers.
- **Carried from June, not re-verified**: `integrations` config table still not in DB — how Jira credentials are actually configured in staging remains unconfirmed (same gap as `business-data-map.md` NEW-G4).
