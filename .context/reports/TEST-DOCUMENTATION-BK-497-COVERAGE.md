# Test Documentation Coverage Matrix — BK-497

> PAT | Require every API route to declare its capability posture
> Generated by `/test-documentation`, ticket-driven scope, 2026-08-20. Modality: jira-native.

## Shape (derive → document → automate)

| Stage | Count | Detail |
|---|---|---|
| Derived (Stage 1 `/sprint-testing` planning) | 17 outlines | Decision Table (AC-04/05/06), BVA (compile-time boundaries), parametrized Decision Table (cookie-only lift), Error Guessing (2 charters) — see `acceptance-test-plan.md` |
| Executed (Stage 2 `/sprint-testing` execution) | 17 + 1 substitute | 15 PASSED, 1 FAILED (→ Improvement BK-542), 1 BLOCKED (→ substitute scenario run instead, PASSED) — see `acceptance-test-results.md` |
| Documented (Stage 4 Phase 3 — persisted in TMS) | 11 Test issues | 9 Candidate + 2 Manual |
| Deferred (report-only, not persisted) | 7 | see table below |

## AC → TC coverage

| AC | Covered by |
|---|---|
| AC-04 (read-only PAT rejected creating invite) | BK-544 (TC-01) |
| AC-05 (write+execute PAT rejected revoking invite) | BK-548 (TC-02) |
| AC-06 (admin PAT, unresolvable workspace, rejected) | Deferred (literal scenario blocked — see below); substitute BK-551 (TC-18, cross-workspace admin PAT rejected) covers the sibling guard branch |
| Headline property (no posture ⇒ compile error) — no formal AC | Verified live in Stage 2 (scratch `tsc --noEmit` fixture), not persisted as a TMS TC — see Deferred (TC-05/06/07/09) |

## Risk-beyond-AC coverage

| Risk | Covered by |
|---|---|
| Positive control — 87-route migration didn't break the allow path | BK-550 (TC-04) |
| Cookie-only lift (the one real behavioural change) — PAT rejected on issue/revoke, unaffected on list/session | BK-552 (TC-10), BK-553 (TC-11), BK-543 (TC-12), BK-545 (TC-13) |
| Auth-resolution ordering (401 before 403) | BK-546 (TC-16) |
| User-facing surface of the cookie-only lift (Settings UI) | BK-547 (TC-14, Manual), BK-549 (TC-15, Manual) |

## Full TC table

| TC | Jira Key | Verdict | Priority (label) | AC/Risk |
|---|---|---|---|---|
| TC-01 | BK-544 | Candidate | Critical | AC-04 |
| TC-02 | BK-548 | Candidate | Critical | AC-05 |
| TC-04 | BK-550 | Candidate | High | beyond-AC (positive control) |
| TC-18 (new — substitute for literal AC-06) | BK-551 | Candidate | Critical | AC-06 sibling branch (cross-workspace rejection) |
| TC-10 | BK-552 | Candidate | Critical | beyond-AC (cookie-only lift, issue-route) |
| TC-11 | BK-553 | Candidate | Critical | beyond-AC (cookie-only lift, revoke-route) |
| TC-12 | BK-543 | Candidate | High | beyond-AC (cookie-only lift, positive control) |
| TC-13 | BK-545 | Candidate | Critical | beyond-AC (cookie-only lift, session-unaffected) |
| TC-16 | BK-546 | Candidate | Medium | beyond-AC (auth-resolution ordering) |
| TC-14 | BK-547 | **Manual** | High | beyond-AC (Settings UI — issue) |
| TC-15 | BK-549 | **Manual** | Critical | beyond-AC (Settings UI — revoke, UI+API integration) |

## Deferred (not persisted in TMS)

| Outline | Scenario | Reason |
|---|---|---|
| TC-03 (literal) | AC-06 — admin PAT with **unresolvable** workspace binding | No viable live-automation path today: issuance blocked by design (`assertTokenIssuanceAuthorized`), QA DB role is read-only, no non-owner admin seed account with 2+ memberships in `.env`. Sibling branch automated instead as TC-18/BK-551. Already covered by the dev's own automated `workspace-context` suite. |
| TC-05 | Compile-time — missing `auth` entirely fails to compile | Type-level property, not QA E2E/API automation scope. Recommend a dev-owned committed `tsd`/`expectTypeOf` test (already noted in `acceptance-test-results.md` Recommendations). |
| TC-06 | Compile-time — empty `requires: []` fails to compile | Same as TC-05. |
| TC-07 | Compile-time — invalid capability literal fails to compile | Same as TC-05. |
| TC-08 | Coverage-check crashes ungracefully on fully-omitted `auth` | Tied to open Improvement BK-542 — bug-driven golden rule says wait until the fix lands, then reuse/create a Test off that closed Improvement, not before. |
| TC-09 | Baseline run of the unmodified coverage-check suite | Dev-owned unit-level test (`lib/api/route-capability-coverage.test.ts`), already exists and passes — redundant to duplicate in the QA TMS. |
| TC-17 | Cross-posture regression spot-check (public/authenticated/required) | One-time bounded sample; dev's own coverage-check + full suite (1555/1556 pass) already own exhaustive regression protection for the 85 migrated call sites. |

## ROI deviation note

11/18 analyzed scenarios (61%) ended up persisted (Candidate + Manual), above the "most should be Deferred" 50% rule of thumb. Justified and confirmed with the user before Phase 3: this Story sits in the CRITICAL Auth/PAT risk tier (`master-test-plan.md`) and the area carries a known prior-bug pattern (GAP-14 residual admin-scoped PATs, `business-api-map.md`) — both trigger the doctrine's prior-bug-rule override.

## Traceability

- All 11 TCs parented to **BK-70 (QA Test Repository)**, component **"Tenancy & Identity"**.
- All 11 linked to **BK-497** via the `Test` link type (empirically verified direction: Test is outward "tests" party, Story is inward "is tested by" — see Engram discovery `discovery/story-test-link-direction`).
- ATP/ATR live on BK-497's Story fields (`customfield_10067` / `customfield_10124`) per jira-native modality — no separate ATP/ATR issue pair.
- Feature-grouping label `epic-bk-1` applied to all 11 (jira-native has no Test Set entity).
- Priority carried via label (`critical`/`high`/`medium`), not a native field — this Jira instance's `Test` issue type has no `priority` field at all (see Engram discovery `discovery/jira-test-issuetype-no-priority`).

## Next steps

- The 9 Candidate TCs (BK-543, BK-544, BK-545, BK-546, BK-548, BK-550, BK-551, BK-552, BK-553) feed `/test-automation` (ticket-driven scope, BK-497). No existing KATA ATC overlap confirmed via `kata-manifest.json` — this is net-new `TokensApi`/capability-guard coverage.
- TC-03's literal AC-06 scenario stays Deferred/blocked — revisit if a scoped DB-write QA role or a dedicated non-owner admin seed account (2+ workspace memberships) becomes available.
- TC-08 stays Deferred until Improvement BK-542 is fixed — then apply the bug-driven golden rule (reuse or create a Test).
- Recommend (non-blocking, from `acceptance-test-results.md`): a committed type-only test (`tsd`/`expectTypeOf`) to lock in the compile-time posture-mandatory property currently only proven by TC-05/06/07's scratch fixture.
