# Risk Assessment

> Generated by: `/project-discovery` Phase 1, sub-step 2 (Project Assessment)
> Target repo assessed: `upex-bunkai-tms`, local checkout at `staging` HEAD `de670c4` (2026-08-14)
> Generated: 2026-08-15
> Scope: HIGH-severity risks only, per Phase 1 completion rule ("If HIGH risks exist, also write `.context/risk-assessment.md`"). MEDIUM/LOW risks are recorded in `CLAUDE.md` §"Project Assessment (Phase 1)" and not duplicated here.

| Severity | Description | Impact | Recommendation | Owner |
|---|---|---|---|---|
| HIGH | No CI/CD pipeline exists in the target repo. `upex-bunkai-tms/.github/workflows/` does not exist; no `vercel.json`. The 138-file `bun test` suite, `bun run lint:check`, and `bun run types:check` only run locally, gated by Husky `pre-commit`/`pre-push` hooks (`lint-staged` + `types:check` + `vars:check` + `skills:check`). | A contributor who bypasses or skips local hooks (`git commit --no-verify`, or simply never runs `bun test` locally) can push code that breaks the build, fails type-checking, or regresses a previously-passing test — with **nothing catching it centrally** before it reaches `staging` or `main`. Given `business-data-map.md` already documents `staging` as 697 commits ahead of `main` with no automated parity check, and this cycle alone shipped 8 new DB tables + an entire execution/defect/notification/milestone domain, the blast radius of an unguarded regression is large and growing. This also means the companion QA repo's regression suites (`/regression-testing`) currently have no CI hook to attach to on the target side — regression runs must be triggered manually against `staging` rather than on every merge. | Wire a GitHub Actions workflow on the target repo that runs, at minimum, `bun run repo:check` (which already exists and bundles format/lint/typecheck/vars/skills checks) plus `bun test` on every PR/push to `staging` and `main`. This is a target-repo engineering task, outside this QA-engineering repo's write scope — surface it to the `upex-bunkai-tms` team rather than attempting to fix it here. | Unassigned — flag to the `upex-bunkai-tms` engineering team/lead. No CODEOWNERS or team roster found to name a specific owner (see `project-config.md` Discovery Gaps). |

## Cross-references

- Full Project Assessment (Testing Maturity, Documentation State, Code Quality, all risks including MEDIUM/LOW): `CLAUDE.md` §"Project Assessment (Phase 1)"
- Repository/environment/access facts: `.context/project-config.md`
- Product-level "silent killer" risks already known from prior business-data-map/master-test-plan regenerations (cron-less stuck-job risk, GAP-14 pre-fix admin PATs, etc.) are **not** re-listed here — they live in `.context/business/business-data-map.md` §9 and `.context/master-test-plan.md` §11, and are QA/product risks rather than this Phase 1 assessment's engineering-infrastructure finding.
